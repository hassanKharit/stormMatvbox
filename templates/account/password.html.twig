{% extends 'base.html.twig' %}

{% block title %}Modifier mon de passe{% endblock %}

{% block content %}
    <h1>Modifier mon mot de passe </h1>
    <a href="{{ path('app_account_password') }}">Retourner a mon compte</a><br/>
    <hr>
    {% if notification is defined %}
        <div class="alert alert-info" > {{notification}} </div>
    {% endif %}
    {{ form(form) }}
    <hr>
{% endblock %}